# Handoff：插件端 · BIsArc 修正后的重导验证与缓弧校准

日期：2026-09-04
提出方：Python 转换器侧（E:\ydb2revit_exe，提交 4e264c8 已推送）
致：CreateNewExtern 插件端
前置：《handoff-Python端-BIsArc误报漏报修正.md》（贵方提出，其 §6 为我方落实记录）
好消息：**本轮插件端零代码改动**——待办只有重导验证与校准协作。

---

## 1. 数据侧现状（已部署两个部署位的最新 exe）

BIsArc 识别规则已按贵方 §3 建议修正并实测：

| 项 | 旧 | 新 |
|---|---|---|
| 规则 | ≥3 段 + 节点转角 0.03°~15° | ≥3 段 + 节点转角 **0.1°~40°** + **转向单调同号** + **累计转角 ≥1.5°** |
| 颛桥 BIsArc=1 | 60 段（大量误报） | **12 段 / 4 条弧链**（累计转角 19°~35°） |
| 贵方 §1 误报样本（tbl1 87/88/89/174/49/95） | 被误标 | **全部清零** ✓ |

12 段清单（tbl1 ID 及弦线端点，供核对）：
① (1841,123857)→(5527,139171) 三段；② (50241,124957)→(53882,142607) 五段；
③ (50297,130257)→(53882,147257) 方向四段；④ (87441,124957)→(90682,142632) 三段。

## 2. 需要插件端做的

### 2.1 重导验证（可与 2.2 合并一轮，也可先行）

用最新 exe 重转颛桥 → **干净新模型**重导 → 核对红标：

1. 原误报的 ~48 根红梁消失（含贵方抽查的近直长梁）；
2. **恰 12 根**弧梁独立成根且染红（位置=§1 清单）；
3. `result.arc_beam_segments = 12`（转换 JSON）与模型红梁数一致。

### 2.2 缓弧校准协作（用户点名的 241101/241095）

现状：这两处弧梁的弦线单节点转角 <1°（贵方合并容差内），被合并成单根直梁且未标红——
新规则的"累计转角 ≥1.5°"下限可能仍排除它们。**待建模侧确认位置**（我方已锁定
两处疑似：`(43082,142632)→(64682,142632)` 方向 4 段、累计 ~0.84°）后：

1. 我方调整判据（降累计下限或增加"等分弦均匀性"）并部署新数据；
2. 贵方再次重导验证该两处：分段保留（不再合并成直梁）+ 染红；
3. 最终基准：建模侧在 YJK 统计的弧梁总数 = `arc_beam_segments` = 模型红梁数。

### 2.3 后续不阻塞项（贵方排期）

* 梁合并三趟 ~77s 的性能优化（贵方已有缓存优化基础，联调日志实测数字在
  《总结-20260904-插件端修改与转换器侧待办handoff.md》§五.3 引用）。

## 3. 明确无需插件端做的（避免重复劳动）

| 项 | 状态 |
|---|---|
| 柱顶方案乙 | ✅ 已上线并经项目3 联调（39 根正常，222500 待用户最终复核） |
| 弧梁染色链路（独立成根→Info 标记→红标） | ✅ 已上线，按数据忠实执行 |
| 墙红标逻辑 | ✅ 已上线（13 段）；节点分量数据修正后自动命中 |
| BIsArc 规则修正 | ✅ 数据侧完成（本 handoff §1），贵方零改动 |

## 4. 验收标准（合并一轮时）

1. 误报红梁清零；12 段强弧红标位置正确；
2. （缓弧数据到位后）241101/241095 对应处分段保留且红标；
3. 三方数字一致：YJK 弧梁总数 = result.arc_beam_segments = 模型红梁数；
4. 其余构件（柱顶/墙红/BZOffset 交叉校验 0 WARN）不回退。

## 5. 部署状态

| 部署位 | 版本 |
|---|---|
| ydb转换.exe（两个部署位） | 4e264c8 构建（BIsArc 新规则 + 此前全部契约） |
| CreateNewExtern.dll | 贵方 e25e167（柱顶乙+弧染色+墙红标）——无需更新 |
