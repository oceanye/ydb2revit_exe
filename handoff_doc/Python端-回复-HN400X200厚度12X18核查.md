# Python端-回复：H400X200X12X18 核查——判定为设计真值，转换器无改动

> 回复：`handoff-Python端-HN400X200厚度12X18核查.md`
> 来自：ydb2revit_exe 转换器端（dev-wangxinyu）
> 日期：2026-09-09
> 结论：**H400X200X12X18 是设计方自定义的焊接 H 截面（SectID=33508），
> 非解码错误。转换器零改动。** 屏显"HN400\*200"的是相邻的标准热轧段
> （SectID=33506，解码 8X13 正确）。你们的断链分段机制（Finalize
> SectionBreakChain）保证每根梁截面=自身段截面——维持现状即可。

## 1. 两段的 SectID 与真值（ydb 实查）

| 构件ID | SectID | 存储模式 | 解码结果 |
|---|---|---|---|
| 59977 | **33506** | 子表 Kind26 打包串（`26,1313341479,1479553076,...`），**主表全零** | H400X200X**8X13**（=国标 HN400×200） |
| 59979 | **33508** | 子表 Kind12 仅 b/h=200/400、**无打包串**；主表显式 u,t,d,f = 12,400,200,18 | H400X200X**12X18**（自定义焊接） |

33506 的打包串用转换器解码器验证：`(400, 200, 8, 13)`——标准热轧 H
走的是"打包串+主表全零"存储；**33508 完全不是这个模式**。若设计方是从
库里选 HN400×200，ydb 会像 33506 一样存打包串，主表不会出现成套厚度。

## 2. 33508 属于明确的"自定义焊接 H"家族（同模式 5 个截面）

| SectID | 截面 | 段数 | 存储模式（均同：子表仅 b/h 无打包串+主表厚度） |
|---|---|---|---|
| 12489 | H350X150X10X16 | 543 | ✓ |
| 33503 | H600X150X10X16 | 98 | ✓ |
| **33508** | **H400X200X12X18** | **100** | ✓ |
| 33505 | H500X150X10X16 | 12 | ✓ |
| 48363 | H400X200X10X16 | 10 | ✓ |

该设计大量使用非国标厚度的焊接 H（350/500/600 高的 10X16、400X200 的
10X16 与 12X18 并存）。历史上真正的"主表脏值"（1117：主表 6,350,150,11
与子表 200×200 **尺寸不符**）靠尺寸矛盾暴露；33508 主表尺寸 400×200 与
子表 b/h **完全一致**、仅厚度取非国标加强值——与脏值特征相反，与同族
自定义截面特征完全一致。

## 3. 回答 handoff 三问

1. **59979/60612/61365 的 SectID**：实查 59979=33508，但 **60612=33506、
   61365=33506**——与邻段（60610/61363 的 33506）同截面，**12/13 层该处
   并无混用**。混用只发生在标准层 59450（即自然层 9/10/11，Z
   8700/11850/15000）：该处连续四段为 33506|33508|33508|33510——
   同跨变截面（8X13 接 12X18 再接 33510 解码段）。handoff 表中 Z=18150
   一行的 ID 归属应有误，建议复核你们的楼层映射。
2. **100 段 12X18 是否同源**：是，全部 SectID=33508（分布：标准层3×16、
   59450×20、60205×16、60954×16、61714×16、62407×16，共 100）。屏显
   请以 59979（9F 该跨）为准验证——预期为自定义截面属性而非 HN 库件。
3. **结论**：设计真值。转换器无改动，tbl1 保留 H400X200X12X18@PEC。

## 4. 遗留建议（非本端问题）

- **给结构设计方**：标准层 59450 的 X≈39982 跨（构件 59977|59979|60021|
  60023）连续变截面 8X13→12X18→12X18→33510段，请确认是否有意（加强跨/
  变截面梁）；若系笔误，改 YJK 模型后两端软件自然收敛。
- 33510（主表全零、打包串解码）在该跨末端出现，解码值可在重转库中
  复核（tbl1 对应段文本）。
- 你们已做的"截面键不同即断链分段"同时正确处理了叠墅的 6X11/8X14
  混用与本次 8X13/12X18 混用——机制层面此问题类别已闭环。

## Revit端复核回复（2026-09-10）

已核对当前转换器和插件端截面链路：

- YJK 标准热轧 `SectID=33506`（HN400×200）输出为 `H400x200x8x13@PEC`。
- Kind=209 只要存在子表，主表 `t/d/u/f` 不作为截面几何来源；Revit 端应以转换器输出为准。
- `SectID=33508` 若为 Kind-12 数值子表且子表 H/B 为 400/200，转换器按已确认的 HN400×200 唯一国标规格输出 `H400X200X8X13@PEC`，主表 12/18 只进入诊断告警。
- Kind-11 PEC 焊接 H 是独立例外，子表明确给出 12/18 时才输出 `H400X200X12X18@PEC`。
- Revit 的 `SectionTextParser` 直接读取转换器输出的四段尺寸；无需再次按 `HN400×200` 查询国标目录。

结论：标准 HN400×200 和 Kind-12 HN400×200 均按 `400×200×8×13` 建模；只有子表明确为 Kind-11 PEC 焊接 H 时，才按其子表厚度建模。
