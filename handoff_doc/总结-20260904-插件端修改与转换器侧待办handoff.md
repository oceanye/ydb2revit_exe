# 总结：标高偏移继承建模一轮（2026-09-04）——插件端修改 + 转换器侧待办 handoff

> **【2026-09-04 更新】** Python 端已回应并落实两项待办，本文档下半部（§五起）
> 为 Python 端补充：已完成/未完成/需插件端事项/部署顺序风险，请插件端与
> 协调人重点阅读 §五.3（重导时机警告）与 §六。

> 插件端仓库：`E:\revit_external_tool2`（分支 dev-wangxinyu，均已推送 gitea）
> 转换器仓库：`E:\ydb2revit_exe`（handoff 已提交至其 dev-wangxinyu，待其实现）
> 模型背景：颛桥 `dtlmodel.ydb`（项目1/项目2 均为它的导入成果）与
> `dtlmodel-斜屋面.ydb`；本文自包含可独立阅读。

## 一、插件端已完成的修改（提交 e25e167，已验证部署）

### 1. 梁标高偏移继承（方案乙：建梁时推导基准）✅ 用户已验证

| 改动 | 内容 |
|---|---|
| 标高解析 | 由"Z 精确匹配标高(1mm)"改为**"距起点 Z 最近的标高，平手取较高者"**——层间梁（降标高梁）不再被跳过 |
| 偏移写入 | 触发条件由"起Z≠终Z"改为"**任一端≠参考标高**"（原条件漏掉两端同降的层间平梁）；统一 `Parameter.Set(double 英尺)`（不再依赖文档显示单位） |
| 交叉校验 | 新增 tbl1 全量 BZOffset 校验：推导基准 vs 列基准 ±1mm，不一致点名——颛桥 7557 段 **0 不一致**，两套规则互为哨兵 |
| 实测 | **110 根层间梁**出现（挂 1900 标高、起点/终点偏移 −1500mm、梁顶 400）；68 根斜屋面斜梁回归不变 |

### 2. 墙顶标高红色标记（按维护方决策：不强求自动修正，人工手改）✅ 用户已验证

| 改动 | 内容 |
|---|---|
| Bug 修复 | `WallImportData.SelectSql` 漏列 WTopZ/WTopZ2——此前可空解析恒 null，墙顶永远走默认（这是首轮"墙顶没变"的根因） |
| 标记逻辑 | 墙仍按默认"上一条标高"建模；**WTopZ 与默认层顶不一致的墙 → 红色实体填充+红色线型**（覆盖全部平面与三维视图）+ 注释写明实际顶标高 |
| 实测 | 颛桥 **10 段**红墙（9 段平顶 400 + 1 段斜顶 400→1900），日志点名 |

### 3. 配套

- 备份节点：实施前打了 `backup-pre-bzoffset-wtopz` 标签（验证通过后已清理，
  其指向的 4cb20e5 本就是分支历史节点）；
- 方案论证文档：《回复-插件端-标高偏移继承建模方案论证与实施计划.md》（本文件夹）、
  对方定案：《Python端-标高偏移继承建模方案论证与实施计划.md》——三项确认全部采纳。

## 二、需要 E:\ydb2revit_exe 修改的（handoff 已交付，待其实现）

### 待办 1：柱顶标高"随梁平齐"未导出（对应项目2 ID 222500 的问题）

**handoff**：`E:\ydb2revit_exe\handoff_doc\handoff-Python端-柱顶标高随梁平齐.md`（提交 edc7474）

- **根因（已实证）**：转换器 tbl2 构造里 **CEndZ 硬编码为楼层顶**；且 ydb 实测
  `tblColSeg.ExtLength / HDiffB` 两份模型 1639 根柱**全为 0**——柱顶随梁属 YJK
  运行时规则，**导出数据里既无标志也无数值**；
- **要求**：YJK 侧先确认规则（典型猜想：柱顶=该节点相连梁的最高梁顶）→
  转换器按规则推导 CEndZ 直写真实顶（不加列，同 WTopZ 思路）；被下调柱在
  result 点名；无数值变化的模型零回归；
- **插件端联动（已承诺）**：数据落地后，柱顶改"最近标高 + 顶部标高偏移"
  （与梁同款方案乙，半天内完成）；
- **附带核查（同源问题）**：3 面墙（Revit 292970/292972/293074 =
  tbl4.ID 177/178/229）用户实测有顶调整，但 ydb `tblWallSeg.HDiff1/2 = 0`
  ——若 YJK 界面已调而导出为 0 属导出缺陷，请 YJK 侧核查答复。

### 待办 2：弧梁标记列 BIsArc（弧梁现以折梁显示，无法标红）

**handoff**：`E:\ydb2revit_exe\handoff_doc\handoff-Python端-弧梁标记列BIsArc.md`（提交 5adac6f）

- **现状（已实证）**：中间库 tbl1 无任何弧信息；`tblGrid.idCen` 全库 0xFFFFFFFF
  空哨兵不可判弧。弧梁在 Revit 中以多段弦线（折梁）显示——合并算法 1° 共线
  容差天然不并它们，每段弦线独立成梁；
- **要求**：tbl1 末尾追加 `BIsArc INTEGER`（0/1），判定标准由 Python 侧按
  YJK 解析说明确定；弦线端点照旧；弧几何参数本期不扩列；无弧模型零回归；
- **插件端联动（已承诺）**：列上线后，合并跳过 BIsArc=1 段使弧梁独立成根，
  染红 + 注释"弧梁:手改"（半小时内完成）。

### 交接状态

两份 handoff 均已提交至 `ydb2revit_exe` 的 `dev-wangxinyu` 分支并推送 gitea，
其实现完成后由插件端做联调验证（颛桥重转 → 重导 → 抽查 222500 柱顶平齐、
弧梁染红）。

## 三、当前部署状态

| 部署位 | 版本 |
|---|---|
| `Addins\2018\CreateNewExtern.dll` | e25e167 构建（梁偏移继承+墙红标） |
| `Addins\2018\数据库\dist\ydb转换.exe` | Python 侧最新（tbl3多塔/PEC截面/BZOffset/WTopZ） |
| 两仓库 dev-wangxinyu | 均已推送 gitea + 本地裸仓库备份 |

## 四、时序建议

1. YJK 侧：答复柱顶规则 + 核查 3 面墙（待办 1 前置）；
2. Python 侧：实现待办 1、待办 2（可并行），重打包部署两处；
3. 插件端：柱顶"最近标高+偏移"、弧梁染色（各自半天/半小时，数据到位即做）；
4. 双方：颛桥联调验收（柱顶平齐 / 弧梁红标 / 3 面墙数据修正后红标补齐）。

---

## 五、Python 端回应（2026-09-04 更新）——已做 / 未做 / 思考

### 5.1 已完成（提交 e1fe390，44 项回归测试全过，exe 已部署两个部署位）

| 来文待办 | 落地情况 | 验收数字 |
|---|---|---|
| 待办 1：柱顶随梁平齐 | ✅ `CEndZ = 层顶 + 该柱节点 tblJoint.HDiff`；result 新增 `column_tops_adjusted`，warnings 点名 SegID 与目标值 | 颛桥 **39 根**柱顶→400（222500 对应 SegID=54228 在列） |
| 待办 1 附带核查：3 面墙 | ✅ 已查明**非导出缺陷**：调整在**端节点 HDiff**（177/178 两端 −1500；229 末端回 0→斜顶）。墙顶公式补节点分量 `WTopZ = 层顶+墙段HDiff+端节点HDiff`（实测两来源互斥、叠加即完整，与梁 Z 公式同构） | 2F 墙顶现为 **12 段 400/400 + 1 段 400→1900**——插件现有红标逻辑**自动命中**，无需改代码 |
| 待办 2：弧梁 BIsArc | ✅ tbl1 末尾追加 `BIsArc INTEGER`（默认 0）；判定=几何弦线链：同层相连 ≥3 段、节点转角 0.03°~15°（共线 0° 不标、构造折梁大转角不标） | 颛桥 **60 段 / 17 链**；result 新增 `arc_beam_segments` |

**关键机制发现（免 YJK 侧确认）**：来文 §2 的前置问题已有答案——柱顶随梁
不在柱段字段（ExtLength/HDiffB 实测全 0 是对的），而在 **tblJoint.HDiff（节点
标高差）**：222500 柱的节点 50856 HDiff=−1500，节点上梁顶=层顶−1500=400，
柱顶同构。梁 Z 公式本就含节点分量，柱/墙补齐后三者同构。

**数字对照澄清**：来文"110 根层间梁"与我方"169 段"不矛盾——前者是合并后
**成根数**（CombineBeam），后者是 tbl1 **段数**。

### 5.2 未完成（明确列出，避免歧义）

| 事项 | 状态 | 说明 |
|---|---|---|
| 弧梁总数与 YJK 核对 | ⏳ 待建模侧 | 请在 YJK 中数弧梁总数，与 60 对齐（来文 §4.1 约定的双方验收数字） |
| 《需求-CSharp插件-PEC墙钢板实体生成》§1.2 修正 | ⏳ 待协调人确认 | 插件端 61afefb 已按"加劲板**成对分居腹板两侧**+腹板中心线对齐"实现（即用户最初表述的构造），需求文档现写"居中横穿"——需改为成对布置使文档与实现一致 |
| 性能两项（`_connection_values` 索引化、RvtID 跨重转换保留） | ⏳ 按分工另行推进 | 本轮明确不纳入；梁合并 77s 提速在插件端 |
| 弧几何参数（圆心/半径/矢高） | ❌ 本期不做 | ydb 无弧元数据，自动生成弧梁需 YJK 导出支持后再评审扩列 |

### 5.3 ⚠️ 部署顺序风险（协调人与插件端必读）

已部署的插件 dll（e25e167）**柱顶仍是"精确匹配标高"**（SqliteDataToRevit.cs:767
找不到即抛异常），而最新部署的转换器已把 39 根柱 CEndZ 写成 400——

> **现在重导颛桥：第一根调整柱即抛"Z=400 找不到顶部标高"，柱阶段整体中断。**

**建议**：插件端先上柱顶方案乙（来文 §3 已承诺，半天），期间**不要重导颛桥**；
到位后用**干净新模型**一次联调四项。若协调人在此之前急需模型，通知 Python 端
临时回退 exe 的 CEndZ 部分（BIsArc 与墙顶分量对现 dll 无害、红标立即可用）。

## 六、需要 E:\revit_external_tool2 做的（数据已全部就绪）
evit_external_tool2 做的（数据已全部就绪）

| # | 内容 | 验收基准 |
|---|---|---|
| 1 | **柱顶方案乙**：顶部标高取距 CEndZ 最近的 tbl3 标高（平手取高）+ `顶部标高偏移` 写真实顶（与梁同款推导） | 39 根柱顶=400；222500 柱顶与梁顶平齐 |
| 2 | **弧梁染色**：合并跳过 `BIsArc=1` 段使独立成根 + 染红 + 注释"弧梁:手改" | 60 段全部独立成根且染红 |
| 3 | 墙红标 | **无需改动**（节点分量上线后 13 段自动红） |

**一次联调验收清单**（干净模型 + 颛桥重转重导）：①柱顶平齐（含 222500）；
②39 根下调柱位置正确；③60 段弧梁独立染红；④2F 红墙 13 段（12 平 1 斜）。

## 七、数据契约现行快照（供插件端查阅）

| 表 | 列（新增均在表尾） | 语义 |
|---|---|---|
| tbl1 | `…, BZOffset, BZOffset2, BIsArc` | 就近层基准偏移（交叉校验）/ 弧梁弦线标记 |
| tbl2 | 无新列 | `CEndZ` 语义升级=真实柱顶（层顶+节点HDiff） |
| tbl3 | 无新列 | 楼层底∪楼层顶（RFn 仅楼层顶脱接，如 RF2@16795） |
| tbl4 | `…, WTopZ, WTopZ2` | 墙两端真实顶=层顶+墙段HDiff+端节点HDiff |

result 机器可读键：`arc_beam_segments`、`column_tops_adjusted`、`warnings`。
