# 总结：Python 端已落实三项（2026-09-04）与插件端联动待办

作者：Python 转换器侧（E:\ydb2revit_exe，分支 dev-wangxinyu，已推送）
对应来文：《总结-20260904-插件端修改与转换器侧待办handoff.md》（插件端）
数据基线：颛桥 `dtlmodel.ydb`；44 项回归测试全过；exe 已重打包并同步两个部署位

---

## 一、本轮已落实的三项（对应来文"待办 1/待办 2/附带核查"）

### 1. 柱顶"随梁平齐"（来文待办 1）——机制已实证并实现

* **关键发现（免 YJK 侧确认）**：柱顶随梁的机制在 ydb 的 **`tblJoint.HDiff`（节点标高差）**：
  ID 222500 柱的节点 50856 HDiff=−1500 → 节点上梁顶=400，柱顶同构。
  （来文查的 ExtLength/HDiffB 全 0 是因为机制在节点表、不在柱段字段；§2 前置问题就此解决）
* **实现**：`tbl2.CEndZ = 层顶 + 该柱节点 HDiff`。颛桥实测 **39 根**柱顶下调至 400；
  result 新增 `column_tops_adjusted` 计数，warnings 点名 SegID 与目标值。
* handoff：《handoff-Python端-柱顶标高随梁平齐.md》§6 已附落实记录。

### 2. 弧梁标记 BIsArc（来文待办 2）——几何弦线链识别

* **判定标准（依来文 §2.2 授权确定）**：ydb 无弧元数据（无弧表、`tblGrid.idCen`
  全 0xFFFFFFFF 哨兵、梁段无标志字段——均已实证）→ **几何识别**：同层首尾相连
  ≥3 段、链上每节点转角 0.03°~15°（共线 0° 不标、构造折梁大转角不标）。
* **实现**：`tbl1` 末尾追加 `BIsArc INTEGER`（默认 0）。颛桥实测 **60 段**（17 条链），
  result 新增 `arc_beam_segments`；弦线端点照旧；无弧模型零回归。
* **验收基准**：请建模侧在 YJK 中数一下弧梁总数，与 60 对齐（来文 §4.1 约定）。
* handoff：《handoff-Python端-弧梁标记列BIsArc.md》§6 已附落实记录。

### 3. 三面墙红标缺失（来文待办 1 附带核查）——同节点机制，已解

* 292970/292972/293074（tbl4 177/178/229）的墙顶调整**不在墙段 HDiff 而在端节点 HDiff**
  （两节点 −1500/−1500；#229 末端节点回 0 → 斜顶 400→1900）——非导出缺陷。
* **实现**：墙顶公式补节点分量 `WTopZ/WTopZ2 = 层顶 + 墙段HDiff + 端节点HDiff`
  （实测两来源互斥、叠加即完整，与梁 Z 公式同构）。
* 颛桥 2F 墙顶实测：**12 段 400/400（原 9 + 弧边 3）+ 1 段 400→1900 + 57 段 1900/1900**
  ——插件现有"WTopZ≠默认层顶→红标"逻辑将**自动**命中这 3 面（含斜顶段），插件端无需改动。

## 二、累计数据契约快照（tbl1~tbl4 现行版）

| 表 | 列变化（均在表尾追加） | 语义 |
|---|---|---|
| tbl1 | `BZOffset, BZOffset2, BIsArc` | 就近层基准偏移（交叉校验用）／弧梁弦线标记 |
| tbl2 | 无新列 | `CEndZ` 语义升级 = 真实柱顶（层顶+节点HDiff） |
| tbl3 | 无新列 | 楼层底∪楼层顶（多塔脱接顶 RFn；无层间标高） |
| tbl4 | `WTopZ, WTopZ2` | 墙两端真实顶 = 层顶+墙段HDiff+端节点HDiff |

result 新增机器可读键：`arc_beam_segments`、`column_tops_adjusted`、`warnings`（柱顶点名/无子表截面点名）。

## 三、需要 E:\revit_external_tool2 修改的内容（按来文已承诺项）

| # | 内容 | 状态依据 |
|---|---|---|
| 1 | **柱顶方案乙**：顶部标高取距 CEndZ 最近的 tbl3 标高（平手取高）+ `顶部标高偏移` 写真实顶（与梁同款）——否则 CEndZ=400 会"找不到顶部标高" | 来文 §3 已承诺（半天）；数据已就绪（39 根） |
| 2 | **弧梁染色**：合并跳过 `BIsArc=1` 段使其独立成根 + 染红 + 注释"弧梁:手改" | 来文 §3 已承诺（半小时）；数据已就绪（60 段） |
| 3 | 墙红标 | **无需改动**——节点分量上线后 3 面弧边墙自动进入红标集合 |

**联调验收（数据全部就绪后一次做齐）**：颛桥重转重导 → ① 222500 柱顶与梁顶平齐（400）；
② 39 根下调柱位置正确；③ 60 段弧梁独立成根且染红；④ 2F 红墙 13 段（12 平 1 斜）。

## 四、遗留（不阻塞上述联调）

* YJK 侧：弧梁总数与 60 的核对；如需自动生成弧梁（非人工手改），弧几何参数
  （圆心/半径/矢高）需 YJK 导出支持——届时再评审扩列；
* 性能议题（`_connection_values` 索引化、RvtID 跨重转换保留、梁合并提速）
  按前期分工另行推进，本轮未纳入。
