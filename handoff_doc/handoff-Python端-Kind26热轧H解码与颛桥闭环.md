# Handoff：Kind=26 热轧 H 型钢打包串解码落地与颛桥闭环（含他方待办）

更新日期：2026-09-02
依据：`YJK Kind26热轧H型钢与Kind209_YDB解析说明.md`（YJK 侧提供）+ 颛桥
`dtlmodel.ydb` 实测数据。
前置：《handoff-Python端-颛桥PEC梁209截面无尺寸排查.md》§5 的待决事项——
**已按本文件解决**（打包串已解出，为标准热轧 HN/HW）。

## 1. Python 端已完成的处理（ydb转换.py，随本 handoff 提交）

当 209 截面主表与子表数值列均无尺寸、子表 `ShapeVal` 为 Kind-26 打包串时，
按解析说明解出真实 H 截面，校验链全部通过才输出：

```text
[0]=26 → [1..8]解包32字节：前2字节选择器必须=39 → 名称(HN400X200…)
       → [9]/65536=B、[10]/65536=H（Q16 定点）→ 名称中的 H/B 与 Q16 交叉一致
       → [14]=0（标准热轧，非自定义）→ [41]=截面 ID 一致
       → 厚度：四段式名称直接取名称；两段式名称查内置 GB/T 11263-2017 规格表
```

任何一环不满足 → 仍然显式拒绝（沿用上一 handoff 的防护，点名 SectID）。

**颛桥全文件实测**（新 exe）：

| 原截面 | 打包名称 | 输出 | 根数 |
|---|---|---|---|
| 33502 | HN600X200 | H600x200x11x17@PEC | 30 |
| 33504 | HN500X200 | H500x200x10x16@PEC | 117 |
| 33506 | HN400X200 | H400x200x8x13@PEC | 731 |
| 33507 | HN400X150 | H400x150x8x13@PEC | 105 |
| 33509 | HN300X150 | H300x150x6.5x9@PEC | 95 |
| 33510 | HW200X200 | H200x200x8x12@PEC | 50 |

转换 result：7557 梁 / 1759 柱 / 229 墙肢；`209,*` 垃圾截面串 **0 条**；
tbl3 含 `RF2@16795`（多塔修复同时在位）。回归测试 35/35 通过。

## 2. Revit 插件端（CreateNewExtern）待办

1. **无需代码改动**：tbl1 契约不变，此前报错的梁现在输出标准
   `H{h}x{b}x{tw}x{tf}@PEC` 字符串；
2. **联调验证**：用新版 ydb转换.exe（两个部署位已更新）重转颛桥
   `dtlmodel.ydb` → 重跑"合并梁并生成模型"：
   * 原 ID=1080 顶部标高错误应消失（tbl3 RF2）；
   * 原"ShapeVal增强失败 / 无法获取截面信息"应消失（截面已可解析）；
3. 若日后仍出现 `209,*` 形态的截面串，属异常数据，按错误处理并回报。

## 3. 建模 / YJK 侧待办

1. **厚度复核（唯一开放项）**：两段式名称的腹板/翼缘厚取自 Python 端内置的
   GB/T 11263-2017 常用规格表（HW100~400、HM148~594、HN198~900）。
   本机无 `sscs_default.ydb` 型钢目录，请 YJK 侧从 sscs 目录 HXG 表抽查
   上述 6 个截面厚度是否一致（预期 8/13、10/16、8/13、6.5/9、11/17、8/12）；
2. 自定义**两段式**名称若不在国标表内（如非标 H520X220）仍会显式拒绝——
   届时需提供 sscs 目录文件或改用四段式命名；
3. 按解析说明 §6：Kind26→209 转换会丢失热轧身份，重要工程建议保留
   转换来源清单（JSON 模板见该说明）。

## 4. 边界说明

* 1117 类矛盾数据（主表有值 H350x150x6x11、打包串 HW200X200）：按解析说明
  §4.1 以主表数值为准（现有行为），打包串仅作无值时的回退来源；
* 选择器 ≠39、自定义标志 ≠0、名称与 Q16 不一致、尾部 ID 不符 → 一律拒绝，
  绝不猜测（解析说明 §3.3 校验链的完整落实）。
